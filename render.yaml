# Exported from Render on 2025-04-19T09:23:11Z
services:
- type: web
  name: emmandevicelaravel-3
  runtime: node
  repo: https://github.com/Capstone2OrgAppointmentClinic/emmandevicelaravel
  branch: main
  plan: free
  region: singapore
  buildCommand: composer install; npm install; npm run build; php artisan key:generate;
    php artisan migrate
  startCommand: php artisan serve --host=0.0.0.0 --port=10000
version: "1"
